ARG ZOTONIC_VERSION=latest
FROM zotonic/zotonic-base:${ZOTONIC_VERSION}

# This Docker image is used to run Zotonic from the host file
# system together with a postgresql container.
#
# $ docker-compose run --service-ports zotonic sh

VOLUME /opt/zotonic
VOLUME /etc/zotonic
