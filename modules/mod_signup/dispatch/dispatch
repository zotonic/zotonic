[
	{signup, ["signup"], resource_signup, [{ssl, true}]},
	{signup_confirm, ["signup", "confirm"], resource_signup_confirm, [{ssl, true}]},
	{signup_confirm, ["signup", "confirm", key], resource_signup_confirm, [{ssl, true}]}
].
