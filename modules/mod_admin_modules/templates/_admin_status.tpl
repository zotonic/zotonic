<div class="clearfix">
    {% button text=_"Rescan modules" action={module_rescan} %}
    <span class="expl">{_ Rescanning will rebuild the index of all modules, actions, templates etc.  It will also reload all dispatch rules. _}</span>
</div>
