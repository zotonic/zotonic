See:
- https://www.postgresql.org/docs/current/functions.html
- https://www.w3.org/TR/sparql11-query


----------------------------------------------------------------------------------------------------
To check:
----------------------------------------------------------------------------------------------------

STR(E)
- problem with RDF type mapping, we could do a 80/20 by just mapping psql types?
  check: mapping of JSON types?

LANG(E)
- We don't have RDF @lang support - something to figure out

LANGMATCHES(E, E)
- Same lang support problem

STRLANG(E,E)
- Lang support / trans rec problem

DATATYPE(E)
- Expects RDF data type... which if of course quite tricky (and needs lang support?)
  Maybe we could simulate some basic psql types?  xsd:string, rdf:langString
  https://www.w3.org/TR/sparql11-query/#func-datatype

IRI(E)
- Resolves a string against the base IRI of the query, we might be able to do this
  on constant strings. Otherwise we need a psql function, which we can supply the
  base IRI of the query.

URI(E)
- ??? Was in the grammar, do not see it in https://www.w3.org/TR/sparql11-query

BNODE( (E) | NIL )
- Constructs a blank node, which is not a normal psql type
  Maybe we need to add sparql types to psql?
  https://www.w3.org/TR/sparql11-query/#func-bnode

ENCODE_FOR_URI(E)
- URI escape function, uses percent encoding of special characters.
  We could use a solution like a stored proc?
  https://stackoverflow.com/a/30771880
  https://stackoverflow.com/a/60260190

TIMEZONE(E)
TZ(E)
- check diff with psql tz support

UUID
STRUUID
SHA1(E)
SHA256(E)
SHA384(E)
SHA512(E)
- Needs psql extensions, check if we want to add this dependency?

STRDT(E,E)
- Literal with rdf type construction...
  https://www.w3.org/TR/sparql11-query/#func-strdt

sameTerm(E,E)
- term comparison with deep nesting? how to compare?
  https://www.w3.org/TR/sparql11-query/#func-sameTerm

isIRI(E)
isURI(E)
- we use rsc-ids, so no direct test
  For strings we might add a stored proc with some RE matching
  Example IRI/URIs: <mailto:a@a.com>  <https://a.com/foo>

isBLANK(E)
- might do on a static, but no rdf typing yet...

isLITERAL(E)
- maybe just a test for number/string/boolean type?
  special care for lang types...

isNUMERIC(E)
- maybe test for int/float types? ok for json?


----------------------------------------------------------------------------------------------------
Partially done  (see z_sparql_sql_function.erl)
----------------------------------------------------------------------------------------------------

REPLACE(E,E,E(,E)?)
- SPARQL uses XPath expressions, and psql uses posix. So we will have differences between them.
  If we map the arguments in XPath ($1..) to what psql uses (\1..) then we come a long way.
  Also needs some mapping of the flags, overall I think we will get a good 80/20 with this one.

REGEX(E,E(,E)?)
- Same regexp difference as replace, 80/20 is ok for now.

STRENDS(E,E)
- starts_with + reverse -- might need better solution?


----------------------------------------------------------------------------------------------------
Done  (see z_sparql_sql_function.erl)
----------------------------------------------------------------------------------------------------


BOUND(Var)          IS NOT NULL
RAND                random
ABS(E)              abs
CEIL(E)             ceil
FLOOR(E)            floor
ROUND(E)            round
CONCAT(Es)          concat (special care for empty case)
SUBSTR(E,E(,E)?)    substr/2 and substr/3
STRLEN(E)           char_length
UCASE(E)            upper
LCASE(E)            lower
CONTAINS(E,E)       strpos > 0
STRSTARTS(E,E)      starts_with
STRBEFORE(E,E)      Use strpos to find pos, then left(s, pos-1)  (can't use split_part..)
STRAFTER(E,E)       Use strpos to find pos, then substr(s, pos+len(b))  (can't use split_part..)
YEAR(E)             year
MONTH(E)            month
DAY(E)              day
HOURS(E)            hour
MINUTES(E)          minute
SECONDS(E)          second
NOW                 current_timestamp / now()
MD5(E)              md5
COALESCE(Es)        coalesce
IF(E,E,E)           case when ... then ... else ... end


----------------------------------------------------------------------------------------------------
Check these, probably mapping could be done using similar psql constructs
----------------------------------------------------------------------------------------------------

I think aggregrate functions can be mapped quite directly to psql aggregates.
Think of max/min/any/etc.  ('sample' doesn't have an equivalent afaik)
- https://www.w3.org/TR/sparql11-query/#aggregateAlgebra
- https://www.w3.org/TR/sparql11-query/#func-filter-exists

--> Need to add support in grammar (yrl), plan and sql mapping.



ExistsFunc    ::=   'EXISTS' GroupGraphPattern

NotExistsFunc     ::=   'NOT' 'EXISTS' GroupGraphPattern

Aggregate     ::=     'COUNT' '(' 'DISTINCT'? ( '*' | Expression ) ')'
                | 'SUM' '(' 'DISTINCT'? Expression ')'
                | 'MIN' '(' 'DISTINCT'? Expression ')'
                | 'MAX' '(' 'DISTINCT'? Expression ')'
                | 'AVG' '(' 'DISTINCT'? Expression ')'
                | 'SAMPLE' '(' 'DISTINCT'? Expression ')'
                | 'GROUP_CONCAT' '(' 'DISTINCT'? Expression ( ';' 'SEPARATOR' '=' String )? ')'
