
.. include:: meta-ping.rst

Simple controller for connection tests, used on the ``/test/connection`` page.

It always responds with the four character string ``pong``.
The ususal path is: ``/.zotonic/ping``