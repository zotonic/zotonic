%% -*- mode: erlang -*-

{erl_opts, [
    debug_info
]}.


{deps,
 [
  zotonic_core,
  zotonic_mod_admin,
  {termit, "2.0.1"}
 ]
}.

{plugins, []}.

{xref_checks, [undefined_function_calls,
               locals_not_used,
               deprecated_function_calls]}.
