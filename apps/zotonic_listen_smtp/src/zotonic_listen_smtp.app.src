%% -*- mode: erlang -*-
{application, zotonic_listen_smtp,
 [{description, "Zotonic SMTP Listener"},
  {vsn, {file, "../../VERSION"}},
  {registered, []},
  {applications, [
      kernel, stdlib, lager, gen_smtp,
      zotonic_core
  ]},
  {env, []},
  {modules,
   [
    zotonic_listen_smtp
   ]}
 ]}.
