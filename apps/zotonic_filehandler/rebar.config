%% -*- mode: erlang -*-

{erl_opts, [
  {parse_transform, lager_transform}
]}.

{deps,
 [
  zotonic_filewatcher,

  {lager, "3.2.1"},
  {erlexec, "1.7.1"}
 ]
}.

{plugins, []}.

{xref_checks, [undefined_function_calls,
               locals_not_used,
               deprecated_function_calls]}.
