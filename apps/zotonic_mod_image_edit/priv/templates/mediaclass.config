% Media classes for editing images
[
    % This is the image we are showing when editing images.
    % It shows the original image, without the image-edit filters applied.
    {"image-edit", [
        % original,
        {width, 2000},
        {height, 2000}
    ]}
].
