%% -*- mode: erlang -*-

{erl_opts, [
    debug_info
]}.


{deps, [
    zotonic_core,
    {shotgun, "0.5.2"}
]}.

{xref_checks, [
    undefined_function_calls,
    locals_not_used,
    deprecated_function_calls
]}.

{dialyzer, [
]}.
